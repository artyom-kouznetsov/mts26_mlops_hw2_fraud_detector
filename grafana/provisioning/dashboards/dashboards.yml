apiVersion: 1

providers:
  - name: Fraud detector dashboards
    orgId: 1
    folder: Fraud detector
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
